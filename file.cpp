#include <stdio.h>
#include "text.h"

int main()
{
    showtext();
    // FILE *fin, *fout;
    // fin = fopen("files/1.txt", "r");
    // fout = fopen("files/2.txt", "w");
    // char ch;

    // int init = ftell(fin);
    // while (true)
    // {
    //     ch = fgetc(fin);
    //     if (ch == EOF)
    //         break;

    //     printf("%c %d \n", ch, ftell(fin));
    //     fputc(ch, fout);
    // }
    // printf("file copied successfully\n");
    // fclose(fin);
    // fclose(fout);
}